name: iphone_homepage
description: A simple Flutter homepage for iPhone with a few buttons.
publish_to: 'none'
version: 0.0.1
environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.5

flutter:
  uses-material-design: false
